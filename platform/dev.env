# 本地开发覆盖层 —— 只覆盖服务端连接串的主机名。
#
# 为什么需要这个文件：.env 里的连接串用的是 Docker 网络主机名（postgres、elasticsearch、
# ai-adapter…），这些名字只在 compose 网络内部可解析。当服务作为本机进程运行时必须换成
# localhost。三个基础设施容器的端口本来就已映射到 127.0.0.1，所以本机进程直接连得上。
#
# 用法：make dev-* 目标会先加载 .env 再把本文件叠加在上面，因此密钥仍然只存在于 .env，
# 本文件不含任何凭据，可以安全提交。
#
# NEXT_PUBLIC_* 系列不在这里覆盖：它们是浏览器侧读取的，本来就已经是 localhost。

DATABASE_URL=postgresql://searchops:searchops_local_only@localhost:5432/searchops
REDIS_URL=redis://localhost:6379
ELASTICSEARCH_URL=http://localhost:9200

SEARCH_SERVICE_URL=http://localhost:8080
COMMERCE_SERVICE_URL=http://localhost:9000
AI_ADAPTER_URL=http://localhost:8000

# commerce 用的是**另一个库**（compose 里给它的是 .../commerce，而搜索侧是 .../searchops），
# 所以只把主机名换成 localhost 还不够——库名也得跟着换，否则 make dev-commerce 会连上
# searchops 库并在迁移阶段失败。demo-up.sh 用 sed 从 DATABASE_URL 派生同样的值；
# 这里显式写出来，让 make dev-commerce 与 demo-up 走同一个结论，不留两处漂移。
COMMERCE_DATABASE_URL=postgresql://searchops:searchops_local_only@localhost:5432/commerce
